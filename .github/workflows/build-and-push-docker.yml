name: build-and-push-docker

on:
  push:
    branches: [ main ]        # pushes to main will publish the image
  pull_request:
    branches: [ main ]        # PRs build only (no push)
  workflow_dispatch:          # manual “Run workflow” button

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write         # required for pushing to ghcr.io

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Optional: enable build cache to speed up subsequent builds
      - name: Build (PRs) / Build+Push (main)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
          tags: |
            ghcr.io/jtn34/user_management:latest
            ghcr.io/jtn34/user_management:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
:wq


